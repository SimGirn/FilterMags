[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "FilterMags"
version = "0.0.1"

dependencies = [
    "os",
    "astropy",
    "numpy",
    "speclite"
]